import PropTypes from "prop-types";

function Bookmodi({ id, userID }) {
  return "null";
}

Bookmodi.propTypes = {
  id: PropTypes.number.isRequired,
  userID: PropTypes.string.isRequired,
};

export default Bookmodi;
